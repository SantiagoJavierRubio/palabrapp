import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(()=>({
    root: {
        
    },
    mainHeader: {
        fontSize: '3rem',
        marginLeft: '1rem',
        fontWeight: 900,
        flexGrow: 1
    }
})
)