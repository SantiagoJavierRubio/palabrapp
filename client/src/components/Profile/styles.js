import { makeStyles } from '@material-ui/styles';

export default makeStyles(()=>({
    errorMessage: {
        marginTop: '2rem',
        textAlign: 'center',
        fontSize: '3rem',
        color: 'salmon'
    }
}))